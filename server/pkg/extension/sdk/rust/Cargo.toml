[package]
name = "filestash"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
mockall = { version = "0.13", optional = true }

[features]
mock = ["dep:mockall"]
