[package]
name = "socket"
edition = "2021"

[lib]
path = "lib.rs"
crate-type = ["cdylib"]

[dependencies]
filestash = { path = "../../../pkg/extension/sdk/rust" }
ureq = { version = "3", default-features = false, features = ["rustls"] }

[profile.release]
opt-level = "z"
lto = true
strip = true
